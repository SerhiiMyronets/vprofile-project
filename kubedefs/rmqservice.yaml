apiVersion: v1
kind: Service
metadata:
  name: vpromq01
  labels:
    app: vprormq
spec:
  selector:
    app: vprormq
  ports:
    - protocol: TCP
      port: 5672
      targetPort: vprormq-port
  type: ClusterIP