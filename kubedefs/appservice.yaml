apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
  labels:
    app: vproapp
spec:
  selector:
    app: vproapp
  ports:
    - name: vproapp-port
      protocol: TCP
      port: 8080
      targetPort: vproapp-port
  type: ClusterIP